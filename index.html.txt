<!-- 替换你 index.html 里的所有内容 -->
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>我的 AI 助手测试</title>
    <style>
        body { font-family: -apple-system, sans-serif; background: #f0f2f5; display: flex; justify-content: center; padding: 20px; }
        .chat-container { background: white; width: 100%; max-width: 500px; border-radius: 15px; box-shadow: 0 8px 30px rgba(0,0,0,0.1); padding: 20px; }
        .input-group { display: flex; gap: 10px; margin-top: 20px; }
        input { flex: 1; padding: 12px; border: 1px solid #ddd; border-radius: 8px; outline: none; }
        button { padding: 10px 20px; background: #000; color: #fff; border: none; border-radius: 8px; cursor: pointer; }
        button:hover { background: #333; }
        .message { background: #e7f3ff; padding: 10px; border-radius: 8px; margin-bottom: 10px; font-size: 14px; }
    </style>
</head>
<body>
    <div class="chat-container">
        <h2>🤖 AI 助手测试版</h2>
        <div id="chat-box">
            <div class="message">你好！我是你的 AI 助手，虽然我现在还没连上大脑（API），但我已经准备好上架 Vercel 了。</div>
        </div>
        <div class="input-group">
            <input type="text" id="user-input" placeholder="请输入你的问题...">
            <button onclick="send()">发送</button>
        </div>
    </div>

    <script>
        function send() {
            const input = document.getElementById('user-input');
            const box = document.getElementById('chat-box');
            if (input.value) {
                const msg = document.createElement('div');
                msg.className = 'message';
                msg.style.background = '#f1f1f1';
                msg.innerText = "用户说：" + input.value;
                box.appendChild(msg);
                input.value = '';
                // 这里以后就是接 OpenAI 的地方
            }
        }
    </script>
</body>
</html>
